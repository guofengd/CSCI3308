version: '3.9'
services:
  web:
    image: node:lts
    working_dir: /repository
    # depends_on:
    #   - db
    ports:
      - '3000:4000'
    volumes:
      - ./:/repository
    command: npm start
  # db:
  #   image:
  #   env_file:
  #   expose:
  #     -
  #   volumes:
  #     -
# volumes: